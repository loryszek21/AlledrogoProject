CREATE TABLE Users (
                       user_id SERIAL,
                       username VARCHAR(50),
                       password TEXT,
                       first_name VARCHAR(50),
                       last_name VARCHAR(50),
                       email VARCHAR(100)

);
